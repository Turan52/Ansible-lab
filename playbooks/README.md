# Playbooks - Ubuntu

Ansible playbooks built and tested on Ubuntu.
Same structure as the Fedora playbooks folder but uses apt instead of dnf, and package names differ where needed. Feel free to pick any playbook and adapt it to your own setup.
